@model JamesPetBoarding.ViewModels.PetFormVM
@{
    ViewBag.Title = "Update Pet";
}

<div class="text-center border-bottom pb-4 mb-4">
    <h1> Paws &amp; Reservations, LLC</h1>
    <h2>Update Pet Profile</h2>
    <p class="lead">
        Update pet information for this pet profile.
    </p>
</div>

@using (Html.BeginForm("Update", "Pets", FormMethod.Post, new { @class = "row g-3 justify-content-center" }))
{

    @Html.AntiForgeryToken()

    @Html.ValidationSummary(false, "", new { @class = "text-danger" })

    @Html.HiddenFor(x => x.PetId)

    <div class="col-md-4">
        <label class="form-label">Pet ID</label>
        @Html.TextBoxFor(x => x.PetId, new { @class = "form-control", @readonly = "readonly" })
    </div>

    <div class="col-md-4">
        <label class="form-label">Pet Name</label>
        @Html.TextBoxFor(x => x.PetName, new { @class = "form-control" })
        @Html.ValidationMessageFor(x => x.PetName, "", new { @class = "text-danger" })
    </div>

    <div class="col-md-2">
        <label class="form-label">Species</label>
        @Html.EnumDropDownListFor(x => x.Species, "Select...", new { @class = "form-select" })
        @Html.ValidationMessageFor(x => x.Species, "", new { @class = "text-danger" })
    </div>

    <div class="col-md-2">
        <label class="form-label">Sex</label>
        @Html.EnumDropDownListFor(x => x.Sex, "Select...", new { @class = "form-select" })
        @Html.ValidationMessageFor(x => x.Sex, "", new { @class = "text-danger" })
    </div>

    <div class="col-md-6">
        <label class="form-label">Breed</label>
        @Html.TextBoxFor(x => x.Breed, new { @class = "form-control" })
        @Html.ValidationMessageFor(x => x.Breed, "", new { @class = "text-danger" })
    </div>

    <div class="col-md-3">
        <label class="form-label">Birth Date</label>
        @Html.TextBoxFor(x => x.BirthDate, new { @class = "form-control", type = "date" })
        @Html.ValidationMessageFor(x => x.BirthDate, "", new { @class = "text-danger" })
    </div>

    <div class="col-md-3">
        <label class="form-label">Weight (lbs)</label>
        @Html.TextBoxFor(x => x.Weight, new { @class = "form-control" })
        @Html.ValidationMessageFor(x => x.Weight, "", new { @class = "text-danger" })
    </div>

    <div class="col-md-12">
        <label class="form-label">Veterinarian</label>
        @Html.DropDownListFor(x => x.VetId, Model.VeterinarianSelectList, "Select...", new { @class = "form-select" })
        @Html.ValidationMessageFor(x => x.VetId, "", new { @class = "text-danger" })
    </div>

    <div class="col-md-12">
        <label class="form-label">Notes</label>
        @Html.TextAreaFor(x => x.Notes, new { @class = "form-control", rows = "4", placeholder = "Enter notes" })
        @Html.ValidationMessageFor(x => x.Notes, "", new { @class = "text-danger" })
    </div>

    <div class="col-12 text-center mt-4">
        <button type="submit" class="btn btn-success me-2">Update Pet Profile</button>

        @Html.ActionLink(
            "Cancel",
            "Read",
            "Pets",
            new { petId = Model.PetId },
            new { @class = "btn btn-secondary ms-2" })
    </div>
}