@model JamesPetBoarding.ViewModels.CustomerPetFormVM
@{
    ViewBag.Title = "Disassociate Customer-Pet Association";
}

<div class="text-center border-bottom pb-4 mb-4">
    <h1> Paws &amp; Reservations, LLC</h1>
    <h2>Disassociate Customer-Pet Association</h2>
    <p class="lead">
        Confirm removal of this customer-pet association. The customer and pet records will remain active.
    </p>
</div>

@using (Html.BeginForm("Delete", "CustomerPets", FormMethod.Post, new { @class = "row g-3 justify-content-center" }))
{

    @Html.AntiForgeryToken()

    @Html.ValidationSummary(false, "", new { @class = "text-danger" })

    @Html.HiddenFor(x => x.CustomerId)

    @Html.HiddenFor(x => x.CustomerPetId)

    @Html.HiddenFor(x => x.PetId)

    <div class="col-md-4">
        <label class="form-label">Customer Name</label>
        @Html.TextBoxFor(x => x.CustomerNameDisplay, new { @class = "form-control", @readonly = "readonly" })
    </div>

    <div class="col-md-4">
        <label class="form-label">Pet Name</label>
        @Html.TextBoxFor(x => x.PetNameDisplay, new { @class = "form-control", @readonly = "readonly" })
    </div>

    <div class="col-md-8">
        <label class="form-label">Relationship Type</label>
        @Html.TextBoxFor(x => x.RelationshipType, new { @class = "form-control", @readonly = "readonly" })
    </div>

    <div class="col-md-8 text-center mt-4">
        <button type="submit" class="btn btn-danger me-2">Disassociate Pet</button>

        @Html.ActionLink(
            "Back to Customer Profile",
            "Read",
            "Customers",
            new { customerId = Model.CustomerId },
            new { @class = "btn btn-secondary ms-2" })
    </div>

}
